# 🔐 GLOBAL ENV SECRETS - 2026 Best Practices
# NEVER commit this file to git
# Loaded automatically by Claude CLI

# ┌─────────────────────────────────────────────────────────────┐
# │ MODEL API KEYS (Cloud Services)                             │
# └─────────────────────────────────────────────────────────────┘

# Xiaomi Mimo v2 Flash (Primary - Free, Fast)
export XIAOMI_API_KEY="sk-e834w7r3sm1e40lagworqazxu2q4zcvzkaqsko775vku1fl7"
export XIAOMI_BASE_URL="https://api.xiaomimimo.com/anthropic"

# Kwaitkat Coder (Alternative - Code Generation)
export KWAITKAT_API_KEY="rNpdtyX2EyxLgrR1k28HDyoZ44ZkBgFRGvBOP_WwWfE"
export KWAITKAT_BASE_URL="https://vanchin.streamlake.ai/api/gateway/v1/endpoints"

# OpenRouter (Dolphin 3.0, DeepSeek, etc.)
export OPENROUTER_API_KEY="sk-or-v1-..."  # ADD YOUR KEY HERE
export OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"

# OpenCode (GLM-4.7)
export OPENCODE_API_KEY="..."  # ADD YOUR KEY HERE
export OPENCODE_BASE_URL="https://api.opencode.ai/v1"

# ┌─────────────────────────────────────────────────────────────┐
# │ MCP SERVER KEYS                                             │
# └─────────────────────────────────────────────────────────────┘

# Tavily Web Search
export TAVILY_API_KEY="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"

# Canva API
export CANVA_API_KEY="${CANVA_API_KEY}"

# YouTube API (for video analysis)
export YOUTUBE_API_KEY="..."

# ┌─────────────────────────────────────────────────────────────┐
# │ LOCAL DEVELOPMENT (Ollama)                                  │
# └─────────────────────────────────────────────────────────────┘

# Ollama Server Configuration
export OLLAMA_HOST="localhost"
export OLLAMA_PORT="11434"
export OLLAMA_METAL="1"           # Enable Metal GPU on Apple Silicon
export OLLAMA_NUM_GPU="100"       # Max GPU layers
export OLLAMA_NUM_PARALLEL="1"    # Single model at a time for stability

# ┌─────────────────────────────────────────────────────────────┐
# │ PROJECT-SPECIFIC SECRETS (Override in .env)                 │
# └─────────────────────────────────────────────────────────────┘

# Database
export DATABASE_URL="postgresql://localhost/mydb"

# API Keys for specific projects
export PROJECT_API_SECRET="..."
export PROJECT_WEBHOOK_SECRET="..."

# ┌─────────────────────────────────────────────────────────────┐
# │ CLAUDE CLI CONFIGURATION                                    │
# └─────────────────────────────────────────────────────────────┘

# Claude CLI Settings (Auto-loaded) - KWAIKKAT CODER CONFIGURATION
# Skip Onboarding Assistant
export CLAUDE_CODE_SKIP_ONBOARDING="true"

# Kwaitkat Coder Configuration (Anthropic Compatible)
export ANTHROPIC_BASE_URL="https://vanchin.streamlake.ai/api/gateway/v1/endpoints/kat-coder-pro-v1/claude-code-proxy"
export ANTHROPIC_AUTH_TOKEN="${KWAITKAT_API_KEY}"
export ANTHROPIC_API_KEY="${KWAITKAT_API_KEY}"
export ANTHROPIC_MODEL="kat-coder-pro-v1"
export ANTHROPIC_SMALL_FAST_MODEL="kat-coder-pro-v1"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kat-coder-pro-v1"
export ANTHROPIC_DEFAULT_OPUS_MODEL="kat-coder-pro-v1"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="kat-coder-pro-v1"

# Timeout & Performance
export API_TIMEOUT_MS="3000000"
export CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC="1"

# IMPORTANT: These local Xiaomi Mimo variables are DISABLED to use Kwaitkat
# export XIAOMI_MIMO_BASE_URL="http://localhost:11434"
# export XIAOMI_MIMO_MODEL="mimo-v2-flash"
# export XIAOMI_MIMO_API_KEY="kwaitkat-local-2026"
# export OPENAI_BASE_URL="http://localhost:11434/v1"
# export OPENAI_API_KEY="kwaitkat-local-2026"
# export OPENAI_MODEL="mimo-v2-flash"

# ┌─────────────────────────────────────────────────────────────┐
# │ DEVELOPMENT TOOLS                                           │
# └─────────────────────────────────────────────────────────────┘

# GitHub
export GITHUB_TOKEN="..."

# Vercel
export VERCEL_API_TOKEN="..."

# Docker
export DOCKER_REGISTRY="..."

# ┌─────────────────────────────────────────────────────────────┐
# │ SECURITY & VALIDATION                                       │
# └─────────────────────────────────────────────────────────────┘

# Validate all keys are set (run: source global.env && /check)
# All keys should be updated before use

# ───────────────────────────────────────────────────────────────
# ⚠️  SECURITY NOTICE:
# - NEVER commit this file to git
# - Add to .gitignore: echo "global.env" >> ~/.gitignore
# - Keep backup in secure location
# - Rotate keys regularly
# ───────────────────────────────────────────────────────────────

# ✅ Status: Ready for use
# 📅 Last Updated: 2026-01-11
# 🎯 Version: 2.0 - 2026 Best Practices